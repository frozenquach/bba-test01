npm init Playwright@latest //Khởi tạo project PlayWright
git init //Khởi tạo git local
git remote add origin <remote url> //Kết nối tới repo của 
ssh-keygen -t rsa -b 4096 -C "your_email@example.com" //Tạo SSH key để connect tới Github
